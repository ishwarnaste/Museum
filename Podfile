# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Museum' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Museum
	pod 'RealmSwift'
  pod 'SwiftyJSON', '~> 4.0'
  pod 'Kingfisher', '~> 5.0'

  target 'MuseumTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MuseumUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
